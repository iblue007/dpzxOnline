<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 实心 -->
    <solid android:color="@color/translucent" />
    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="#56d0e3" />
    <!-- 圆角 -->
    <corners android:radius="5dp" />
    <!-- 边距 -->
    <padding
        android:bottom="8dp"
        android:left="15dp"
        android:right="15dp"
        android:top="8dp" />

</shape>